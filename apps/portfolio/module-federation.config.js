module.exports = {
  name: 'portfolio',
  remotes: ['resume-remote'],
  // shared: (name, config) => {
  //   if (['@angular/core', '@angular/router'].includes(name)) {
  //     console.log(name, config)
  //     return config
  //   }
  //   return false
  // },
}
